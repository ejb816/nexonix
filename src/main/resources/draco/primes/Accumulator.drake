type Accumulator from Primes
  elements
    fix primeSet {Int}+
    fix compositeSet {Int}+
    fix naturalSet {Int}+
    fix intervalTextSet {(Long, String)}+
  factory
    body
      fix primeSet {Int}+ {}
      fix compositeSet {Int}+ {}
      fix naturalSet {Int}+ {}
      fix intervalTextSet {(Long, String)}+ {}
domain draco primes Primes
