type EmissionReceived
domain draco generator Generator
rule
  pattern
    variables
      var emission Emission
  action
    loc received [Emission]+ ctx.getRuntime().get("received")
    mon add(received, emission)
