type Rule(T) from RuleType
  elements
    fix pattern (Knowledge -> Unit)
    fix action (RhsContext -> Unit)
  factory
    parameters
      par pattern (Knowledge -> Unit)
      par action (RhsContext -> Unit)
  globals
    fix knowledgeService KnowledgeService new KnowledgeService ()
domain draco Draco
