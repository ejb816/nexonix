{
  "name": "AddSequence",
  "variables": {
    "fp": "Json"
  },
  "conditions": [],
  "values": {
    "delta": ["fp","delta"],
    "inputCount": ["fp","inputCount"]
  },
  "action": [
      "for (i <- 2 to inputCount.value[Int]) { context.insert(i) }",
      "context.insert(FindPrimes(inputCount.value[Int], delta.value[Int]))"
  ]
}
