type DomainDictionary from Dictionary(DomainType, TypeDictionary)
  factory
    parameters
      par domains [DomainType] []
    body
      fix kvMap {DomainType, TypeDictionary} _domains.map(domain => (domain, domain.typeDictionary)).toMap
domain draco Draco
