type Emit
domain draco gendrake GenDrake
rule
  pattern
    variables
      var definition TypeDefinition
  action
    fix emission draco.generator.Emission draco.generator.Emission(draco.Drake.emit(definition))
    mon ctx.insert(emission)
