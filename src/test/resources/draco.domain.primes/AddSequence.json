{
  "name": "AddSequence",
  "variables": {
    "fp": "Json"
  },
  "conditions": [],
  "values": {
    "delta": ["fp","delta"],
    "maximum": ["fp","maximum"]
  },
  "action": [
      "for (i <- 2 to maximum.value[Int]) { context.insert(i) }",
      "context.insert(FindPrimes.define(maximum.value[Int], delta.value[Int]))"
  ]
}
