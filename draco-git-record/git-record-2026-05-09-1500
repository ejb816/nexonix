cd /Users/edwardburke/IdeaProjects/nexonix

# New file from this session — Stage 2e foundation journal chapter
git add draco-dev-journal/draco-dev-chapter-28.md

# New file from this session — git-record audit trail
git add draco-git-record/git-record-2026-05-09-1500

# All modifications and the already-staged new files (the 6 aspect/typedef JSONs
# generated by YamlToJsonBootstrap, the new test file itself, the Generator
# rewrite, the cleaned TypeDefinition.scala, the JSON authoring updates for
# the Draco core types, and the 26 reference-frame domain files patched with
# `with DracoType` to match Generator output).
git add -u

git commit -m "$(cat <<'EOF'
Stage 2e foundation — whole draco.* package generatable from JSON; DracoType-as-root architectural shift; TypeDefinition.scala back-compat purge.

Original session goal was Stage 2e leaf detection; never reached. The session became a deep audit of TypeDefinition.scala bootstrap soundness, then a one-type-at-a-time push to make the entire draco.* package JSON-generatable, then the architectural shift to DracoType-as-root with every instance carrying typeDefinition.

Decision committed: every type in draco.* extends DracoType; every instance carries val typeDefinition: TypeDefinition. Whole-package JSON generation replaces the two-tier Extensible-vs-DracoType root.

TypeDefinition.scala cleanup (user-driven mid-session):
- Trait body: 17 convenience defs (def superDomain = dracoAspect.superDomain, ...) deleted. Trait now declares just val typeName: TypeName; the four aspect vals come via `extends Aspects`.
- fromAspects helper deleted (no JSON counterpart to specify).
- Bootstrap value collapsed to TypeDefinition.Null (no caller reads it; routing through legacy apply with over-counted aspects-as-elements was wrong).
- Decoder routes through the surviving public apply (extracts flat fields from just-built aspect blocks). Awkward aspects→flat→aspects round-trip but uses only the public surface.
- Custom encoder + dual-shape decoder retained as hand-written exceptions (Generator can't emit either).

YamlToJsonBootstrap added (src/test/scala/draco/language/YamlToJsonBootstrap.scala): one-shot test that generates JSON sibling for every YAML file under src/main/resources/draco/, decoding via TypeDefinition decoder and re-encoding via TypeDefinition encoder. Materialized 6 JSON files that had no git history (ActorAspect, Aspects, DomainAspect, DracoAspect, RuleAspect, TypeDefinition) — these were born YAML during Stage 2d's aspect redesign; restoring them as JSON gives the test corpus a ground-truth reference. YAMLRoundTripTest stayed at 118/118 throughout.

GenerateAndCompileTest restructured into three group tests, each compiled as one unit:
- TypeElement hierarchy (12 types: TypeElement + BodyElement + Fixed/Mutable/Dynamic/Parameter/Monadic + Pattern/Action/Condition/Variable/Factory)
- TypeDefinition family (6 types: TypeDefinition + Aspects + DracoAspect/DomainAspect/RuleAspect/ActorAspect)
- Draco core types (26 remaining types in draco/ root)
On compile failure, dumps generated source to /tmp/<group>-generated.scala for diagnosis.

Generator emission gains (12 new capabilities, catalog in project_generator_emission_features_2026-05-09.md):
- factory.body Monadic case: emit raw code, no override prefix (local vals, side-effect statements)
- factory.body Dynamic with method body via methodBody(d.body) (was using d.value which is hardcoded "")
- factory.body Dynamic emits methodParameters (was missing — overrides didn't actually override)
- typeBody Fixed/Mutable concrete-default when value non-empty (was always abstract)
- typeBody Dynamic concrete-with-body when body non-empty (used by Dictionary's delegating methods)
- isFunctionLikeType helper: skip simpleCodecDeclaration when any factory param has Consumer[*]/Function[*]/Behavior[*]/=> type (RuleType has Consumer fields; circe has no implicit codec)
- hasTypeDefinitionOverride: skip auto override lazy val typeDefinition emission when factory params or body already provide a typeDefinition override (Type[T] and RuleType use this)
- Companion `with DracoType` via parents-list pattern when chainHits(td, "DracoType") — companions of DracoType-derived types ARE DracoType themselves
- Discriminated decoder fallback returns Left(io.circe.DecodingFailure) instead of Right($name.Null) (parents without factory don't get a Null emitted)
- Unified nullInstance: always Null = apply(_arg = nullValueFor(...)); apply already does correct overrides via body
- Multi-type generate's merged TD includes _derivation so external imports for cross-package derivation references resolve
- typeExtends DracoType-self-cyclic guard (`else if (td.typeName.name == "DracoType") ""` replaces the Extensible variant)

Generator cascade for the 17 deleted convenience defs in TypeDefinition: ~50 distinct call sites in Generator.scala remapped via replace_all per field name (td.X → td.dracoAspect.X for the 9 DracoAspect fields; td.elementTypeNames → td.domainAspect.elementTypeNames; td.variables/action/etc. → td.ruleAspect.X). Same pattern for parentTd, moduleTd, and underscore-syntax `_.X`. Plus one site in TypeDictionary.scala.

JSON authoring updates (one-type-at-a-time iteration through Draco core failures):
- ContentSink: added derivation [DracoType] + factory.body with Monadic for sinkPath val + Dynamic for write override (FQN for java.nio.file.* to avoid externalTypeImports extension)
- SourceContent: same pattern; factory.body has sourceURI Monadic + 3 Fixed overrides + source.close() Monadic
- Type: renamed factory param _typeDefinition → typeDefinition (auto-emit `_${p.name}` produces correct override)
- RuleType: added action/pattern to elements; renamed factory params (drop leading underscore); factory.body provides typeDefinition = _ruleDefinition (matches hand-written intent)
- Main: added derivation [DracoType] + factory.body with classOf-based URI computation + globalElements for `roots` lazy val
- Test: same pattern with src/test paths
- Actor: added factory.parameters [actorDefinition] + factory.body with typeDefinition/actorDefinition overrides + Dynamic methods receive/receiveSignal returning Behaviors.same[T] (FQN for pekko types)
- Dictionary: added 4 Dynamic delegating methods (removed/updated/get/iterator); for updated[V1 >: V] embedded the type parameter in the name field
- DomainDictionary: factory.body overrides kvMap from _domains
- TypeDictionary: factory.body overrides elementTypes and kvMap from _domainDefinition
- TypeElement: added value: "Seq.empty" defaults to parameters and body Fixed elements (concrete inheritable)
- Pattern/Action/Condition/Variable/Factory/Dynamic/Monadic: factory.body added enumerating the explicit overrides each leaf does in its hand-written apply (matching the inherited TypeElement abstracts)

Hand-written Scala updates (Scala-first for inheritance backbone):
- ContentSink.scala (user-edited): trait extends DracoType, companion extends App with DracoType, Generator.loadType, apply override of typeDefinition
- SourceContent.scala: same pattern
- Main.scala: same pattern; sourceRoot/sinkRoot computed via classOf
- Test.scala: same pattern
- Actor.scala: same pattern; override on receive/receiveSignal/typeDefinition/actorDefinition
- Generated.scala: minimal `override val typeDefinition: TypeDefinition = Generated.typeDefinition` patch (Main now extends DracoType, transitively forces Generated to override)
- TypeDefinition.scala: see cleanup section above
- TypeDictionary.scala: one-line fix `_domainDefinition.elementTypeNames` → `_domainDefinition.domainAspect.elementTypeNames`

Reference frames cascade: 26 hand-written domain Scala files patched (added `with DracoType` to companion `extends App`) to match Generator's new emission. Bulk-applied via sed:
- Cosmocentric (1)
- Egocentric family (12: Egocentric/Direction/Distance/Course/Gaze/Percept/Lean/Effect/Waypoint/Path/Ego)
- Geocentric family (5: Geocentric/Position/Altitude/Heading/Fix)
- Heliocentric family (4: Heliocentric/Elements/Epoch/Ephemeris)
- Galactocentric family (5: Galactocentric/Parallax/ProperMotion/RadialVelocity/Trajectory)

Test state at session close: 156 total, all PASS. GenerateAndCompileTest's three groups verify the whole draco.* package generates and compiles from JSON. ReferenceFramesGenTest 38 reference-frame types match Generator output. YAMLRoundTripTest 118/118.

Cascade still partial: Primal/TypeName/TypeElement/Holon hand-written code still says `extends Extensible`. Eliminating Extensible and migrating those four traits is the natural follow-up — each migration would propagate a chain of typeDefinition overrides through anonymous instances and would benefit from its own focused session.

What's open after this session:
- Stage 2e leaf detection proper — the original goal of this session, foundation now in place
- Eliminate Extensible — finishes the architectural shift; cascade through the four hand-written traits
- Replace hand-written domain files with generated — 26 ref-frame files now byte-matchable
- Reconcile TypeDefinition.yaml with hand-written apply — bootstrap regenerability still blocked

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"

git push
